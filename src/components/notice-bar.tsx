// Notice bar removed in Wave 40 (mediation excision). Stub kept so any lingering
// import resolves cleanly. Safe to delete once all import sites are confirmed clean.
export function NoticeBar() {
  return null;
}
